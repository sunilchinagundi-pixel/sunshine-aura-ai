services:
  - type: web
    name: sunshine-aura-ai
    runtime: docker
    buildCommand: docker build -t sunshine-aura-ai .
    startCommand: docker run -p $PORT:8000 sunshine-aura-ai
    envVars:
      - key: PORT
        value: 8000